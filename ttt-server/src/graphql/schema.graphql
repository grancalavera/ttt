type Game {
  id: String!
}

type Mutation {
  join: JoinResult!
  register: RegisterResult!
}

type Query {
  users: [User!]!
  whoami: String
  ping: String!
}

type RegisterResult {
  accessToken: String!
  user: User!
}

type User {
  id: Int!
  tokenVersion: Int!
}

type JoinResult {
  gameId: ID!
  token: Token!
  next: Token!
}

enum Token {
  O
  X
}

union GameState = GamePlaying | GameDraw | GameWon

type GamePlaying {
  id: ID!
  next: Token!
}

type GameDraw {
  id: ID!
}

type GameWon {
  id: ID!
  winner: Token!
}
