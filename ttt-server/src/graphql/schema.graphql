# import * from 'game-schema.graphql'
# import * from 'user-schema.graphql'

type Mutation {
  registerUser: UserRegisterMutationResult!
  openGame: GameOpenMutationResult!
  startGame(input: GameStartMutationInput!): GameStartMutationResult!
}

type Query {
  whoami: User!
  ping: String!
}

type Subscription {
  gameChanged(channelId: ID!): GameState!
}
