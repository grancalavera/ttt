import React from "react";
import { Button } from "@blueprintjs/core";
import "@blueprintjs/core/lib/css/blueprint.css";

export const App: React.FC = () => {
  return <Button intent="danger" text="install all the viruses" />;
};
