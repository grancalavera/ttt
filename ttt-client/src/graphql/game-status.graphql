query GameStatus($gameId: ID!) {
  gameStatus(gameId: $gameId) {
    __typename

    ... on GamePlaying {
      id
      me
      next
    }

    ... on GameWon {
      id
      me
      winner
    }

    ... on GameDraw {
      id
      me
    }
  }
}
